// CountDown Clock
// Version   : 1.0.1
// Developer : Ekrem KAYA
// Website   : https://e-piksel.com
// GitHub    : https://github.com/epiksel/countdown

!(function (t) { t.fn.countdown = function (e, n) { function o () { let t = new Date(r.date); let e = s(); let o = t - e; if (o < 0) return clearInterval(d), void (n && typeof n === 'function' && n()); let a = 1e3; let f = 60 * a; let u = 60 * f; let l = 24 * u; let c = Math.floor(o / l); let h = Math.floor(o % l / u); let x = Math.floor(o % u / f); let g = Math.floor(o % f / a); let y = c === 1 ? r.day : r.days; let m = h === 1 ? r.hour : r.hours; let v = x === 1 ? r.minute : r.minutes; let D = g === 1 ? r.second : r.seconds; c = String(c).length >= 2 ? c : '0' + c, h = String(h).length >= 2 ? h : '0' + h, x = String(x).length >= 2 ? x : '0' + x, g = String(g).length >= 2 ? g : '0' + g, i.find('.days').text(c), i.find('.hours').text(h), i.find('.minutes').text(x), i.find('.seconds').text(g), i.find('.days_text').text(y), i.find('.hours_text').text(m), i.find('.minutes_text').text(v), i.find('.seconds_text').text(D) } var r = t.extend({ date: null, offset: null, day: 'Day', days: 'Days', hour: 'Hour', hours: 'Hours', minute: 'Minute', minutes: 'Minutes', second: 'Second', seconds: 'Seconds' }, e); r.date || t.error('Date is not defined.'), Date.parse(r.date) || t.error('Incorrect date format, it should look like this, 12/24/2012 12:00:00.'); var i = this; var s = function () { let t = new Date(); let e = t.getTime() + 6e4 * t.getTimezoneOffset(); let n = new Date(e + 36e5 * r.offset); return n }; var d = setInterval(o, 1e3) } }(jQuery))
